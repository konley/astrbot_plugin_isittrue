{
  "cooldown": {
    "description": "用户冷却时间（秒）",
    "type": "int",
    "hint": "同一用户在该时间内重复触发会被拦截",
    "default": 10
  },
  "trigger_phrases": {
    "description": "检测触发词列表",
    "type": "list",
    "hint": "每行一个触发词。@机器人或引用消息时，文本命中任一触发词就会触发；开启前缀/结尾监听后也会用这些词匹配。留空回退为“真的吗”。英文词会按边界匹配，避免误伤。",
    "default": [
      "真的吗"
    ],
    "items": {
      "type": "string"
    }
  },
  "listen_suffix": {
    "description": "监听触发词结尾的消息",
    "type": "bool",
    "hint": "开启后，群内以任一触发词结尾的消息会自动触发（无需@），兼容中英文问号",
    "default": false
  },
  "listen_prefix": {
    "description": "监听触发词开头的消息",
    "type": "bool",
    "hint": "开启后，群内以任一触发词开头的消息会自动触发（无需@）",
    "default": false
  },
  "group_blacklist": {
    "description": "群黑名单",
    "type": "list",
    "hint": "填群号。黑名单内的群不会触发本插件",
    "default": [],
    "items": {
      "type": "string"
    }
  },
  "provider_id": {
    "description": "事实核查模型 Provider ID",
    "type": "string",
    "hint": "留空则使用当前默认 Provider；填写后优先用指定 Provider，不存在时回退默认",
    "default": ""
  },
  "enable_vision": {
    "description": "是否启用图片分析",
    "type": "bool",
    "hint": "需要当前 Provider 模型支持多模态（视觉）",
    "default": true
  },
  "enable_web_search": {
    "description": "启用联网搜索增强",
    "type": "bool",
    "hint": "默认关闭。开启后优先调用 anysearch_search 工具；若无工具则用内置 HTTP 客户端直连 Anysearch（可读 anysearch 插件配置里的 api_key）。未装 anysearch 也不影响本插件加载",
    "default": false
  },
  "search_timeout": {
    "description": "联网搜索超时（秒）",
    "type": "int",
    "hint": "超时后自动回退到仅凭模型知识判断",
    "default": 30
  },
  "max_content_chars": {
    "description": "待核文本最大字符数",
    "type": "int",
    "hint": "超长文本会截断后再送模型，避免撑爆上下文",
    "default": 2500
  },
  "max_search_chars": {
    "description": "搜索资料最大字符数",
    "type": "int",
    "hint": "联网搜索结果写入 prompt 的上限",
    "default": 2000
  },
  "true_label": {
    "description": "判定为真时的展示文案",
    "type": "string",
    "hint": "模型返回 true 时展示",
    "default": "✅ 真的喵"
  },
  "false_label": {
    "description": "判定为假时的展示文案",
    "type": "string",
    "hint": "模型返回 false 时展示",
    "default": "❌ 假的喵"
  },
  "unknown_label": {
    "description": "无法核实时的展示文案",
    "type": "string",
    "hint": "模型返回 unknown 时展示",
    "default": "⚠️ 布吉岛"
  },
  "system_prompt": {
    "description": "事实核查系统提示词",
    "type": "text",
    "default": "你是群聊事实核查助手。你只能依据用户提供的「待核内容」「图片」和「参考资料」作答，不要假装自己刚刚联网搜索，也不要编造不存在的链接或新闻。\n判断原则：\n1. 只核查可验证的事实主张；主观观点、价值判断、玩笑、预测优先 unknown。\n2. 若内容含多条主张，只核查最核心、最可验证的一条，并在解释中点明。\n3. 涉及时效信息（股价、比分、突发新闻、最新政策等）且没有可用参考资料时，优先 unknown，不要用过期记忆硬判。\n4. 图片仅在可辨认关键文字/图表时作为证据；看不清就说明限制。\n5. 必须严格按以下格式输出，第一行只能是单个英文单词：\n第一行：true / false / unknown\n第二行起：中文解释，100字以内，说明依据与不确定点。"
  },
  "max_forward_depth": {
    "description": "合并转发最大嵌套层数",
    "type": "int",
    "hint": "超过后截断并在备注说明。硬上限 8，默认 4，防恶意深层嵌套",
    "default": 4
  },
  "max_forward_nodes": {
    "description": "合并转发最大节点数",
    "type": "int",
    "hint": "统计展开过程中的消息节点。硬上限 100，默认 40",
    "default": 40
  },
  "max_forward_fetch": {
    "description": "合并转发远程展开最大次数",
    "type": "int",
    "hint": "调用 get_forward_msg 的次数上限，含嵌套。硬上限 20，默认 8；重复 id 会跳过",
    "default": 8
  },
  "max_forward_images": {
    "description": "合并转发最多提取图片数",
    "type": "int",
    "hint": "防止超多图打满多模态请求。硬上限 20，默认 8",
    "default": 8
  }
}
